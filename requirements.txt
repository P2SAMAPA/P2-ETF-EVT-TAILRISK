# ── Core (EVT engine) ─────────────────────────────────────────────────────────
pandas>=1.5.0
numpy>=1.23.0
scipy>=1.9.0
huggingface_hub>=0.16.0
streamlit>=1.25.0
plotly>=5.14.0
pyarrow>=10.0.0
matplotlib>=3.5.0

# ── Flow & Positioning module ─────────────────────────────────────────────────
yfinance>=0.2.18
requests>=2.28.0
nasdaq-data-link>=1.0.0   # optional: set NASDAQ_API_KEY secret for longer short history
                           # without this key, FINRA direct API is used (~2yr history)
